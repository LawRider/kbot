# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  #repository: "lawrider"
  registry: "ghcr.io"
  repository: "lawrider/kbot"
  pullPolicy: IfNotPresent
  tag: "v1.0.2-c617be6"
  os: linux
  arch: amd64
  parameter: go
secret:
  name: "kbot"
  key: "token"
  env: "TELE_TOKEN"
